"""One module per facet: what to ask a provider, how to read the answer, what to write.

A facet is the unit the ledger tracks, so the split here matches `ingest.facet` rows rather than
any convenience of the code.
"""
